api_key: ${DD_API_KEY}

logs_enabled: true

log_level: warn

hostname: etc.folder

process_config:
  enabled: "true"

apm_config:
  enabled: true
  analyzed_spans:
    event-list-heroku|rack.request: 1